<?php

return [

    /*
    |--------------------------------------------------------------------------
    | حدود رفع الوسائط من التطبيق
    |--------------------------------------------------------------------------
    |
    | الحدّ الأعلى للملف الواحد بالكيلوبايت. كان 4 ميغابايت، وهو أقلّ من عتبة
    | الضغط في ImageCompressionService (5 ميغابايت) — فصور الهواتف الكبيرة
    | تُرفض عند التحقّق قبل أن يصلها الضاغط الموضوع لها أصلاً. القيمة هنا تترك
    | مجالاً للضغط أن يعمل، ويبقى الحدّ الفعلي محكوماً بـ upload_max_filesize
    | و post_max_size في إعدادات PHP على الخادم.
    |
    */
    'max_upload_kb' => env('CHAT_MAX_UPLOAD_KB', 16384),

    /** أقصى عدد ملفات في الطلب الواحد عند الإرسال دفعة واحدة. */
    'max_batch_files' => env('CHAT_MAX_BATCH_FILES', 10),

    /*
    | حجم ردّ المزامنة (ميغابايت) الذي يستحقّ تحذيراً في السجلّ. الردود الضخمة
    | تموت عند الخادم أو التطبيق لا داخل PHP، فلا يبقى منها أثر يُشخَّص.
    */
    'large_sync_warn_mb' => env('CHAT_LARGE_SYNC_WARN_MB', 4),

];
