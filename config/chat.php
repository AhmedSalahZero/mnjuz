<?php

return [

    /*
    |--------------------------------------------------------------------------
    | حدود رفع الوسائط — متوافقة مع الداشبورد (ChatForm.vue)
    |--------------------------------------------------------------------------
    |
    | الصور: لا سقف نوعي هنا — يُضغط الخادم لحدّ واتساب (5MB) قبل الإرسال.
    | الفيديو والصوت: 16MB. المستندات: 100MB. السقف الفعلي = min(هذه القيم، PHP).
    |
    | max_upload_kb يبقى احتياطاً للأنواع غير المعروفة فقط.
    |
    */
    'max_upload_kb' => env('CHAT_MAX_UPLOAD_KB', 16384),

    'max_upload_kb_by_type' => [
        'video' => env('CHAT_MAX_UPLOAD_VIDEO_KB', 16384),
        'audio' => env('CHAT_MAX_UPLOAD_AUDIO_KB', 16384),
        'document' => env('CHAT_MAX_UPLOAD_DOCUMENT_KB', 102400),
    ],

    /** أقصى عدد ملفات في الطلب الواحد عند الإرسال دفعة واحدة. */
    'max_batch_files' => env('CHAT_MAX_BATCH_FILES', 10),

    /*
    |--------------------------------------------------------------------------
    | سقف الطلب الواحد بالبايت
    |--------------------------------------------------------------------------
    |
    | أصغر حدٍّ في الطريق بين المتصفّح وphp-fpm — وليس بالضرورة حدّ PHP.
    |
    | Cloudflare على الخطط المجانية وPro يرفض أي طلب يتجاوز 100 ميغابايت،
    | ويقطع الاتصال قبل أن تبلغ الحمولة الخادم: لا 413 في سجلّ nginx بل 499،
    | ولا خطأ يصل المتصفّح — يتجمّد شريط التقدّم عند النسبة التي بلغها.
    |
    | ولأن الحدّ خارج PHP فلا سبيل إلى قراءته، فيُعلَن هنا صراحةً. القيمة
    | الافتراضية 90 ميغابايت: دون المئة بهامش يتّسع لحدود multipart وترويسات
    | الطلب. من رقّى خطته إلى Business (200) أو Enterprise (500) يرفعها هنا.
    |
    */
    'max_request_bytes' => env('CHAT_MAX_REQUEST_BYTES', 90 * 1024 * 1024),

    /*
    | حجم ردّ المزامنة (ميغابايت) الذي يستحقّ تحذيراً في السجلّ. الردود الضخمة
    | تموت عند الخادم أو التطبيق لا داخل PHP، فلا يبقى منها أثر يُشخَّص.
    */
    'large_sync_warn_mb' => env('CHAT_LARGE_SYNC_WARN_MB', 4),

];
