# دليل قاعدة بيانات الاختبارات

## ❓ هل يجب إنشاء قاعدة بيانات منفصلة؟

### الإجابة: **نعم، موصى به بشدة!** ✅

---

## 🎯 لماذا قاعدة بيانات منفصلة؟

### المشاكل بدون قاعدة بيانات منفصلة:
1. ❌ **خطر حذف البيانات الحقيقية** - الاختبارات قد تحذف بيانات production
2. ❌ **تلوث البيانات** - بيانات الاختبار تختلط مع البيانات الحقيقية
3. ❌ **بطء الاختبارات** - قاعدة البيانات الكبيرة تبطئ الاختبارات
4. ❌ **مشاكل في CI/CD** - صعوبة في تشغيل الاختبارات في بيئات مختلفة

### المميزات مع قاعدة بيانات منفصلة:
1. ✅ **أمان** - لا خطر على البيانات الحقيقية
2. ✅ **سرعة** - قاعدة بيانات صغيرة = اختبارات أسرع
3. ✅ **نظافة** - كل test يبدأ من الصفر
4. ✅ **سهولة CI/CD** - يعمل في أي بيئة

---

## 🔧 الخيارات المتاحة

### الخيار 1: SQLite في الذاكرة (In-Memory) ⭐ **الأفضل**

#### المميزات:
- ✅ **سريع جداً** - في الذاكرة فقط
- ✅ **لا يحتاج إعداد** - يعمل مباشرة
- ✅ **نظيف** - يختفي بعد انتهاء الاختبار
- ✅ **مثالي للاختبارات البسيطة**

#### العيوب:
- ❌ **لا يدعم بعض MySQL features** - مثل JSON functions معقدة
- ❌ **محدود** - للاختبارات البسيطة فقط

#### الإعداد:
```xml
<!-- phpunit.xml -->
<env name="DB_CONNECTION" value="sqlite"/>
<env name="DB_DATABASE" value=":memory:"/>
```

---

### الخيار 2: SQLite ملف منفصل

#### المميزات:
- ✅ **سريع**
- ✅ **منفصل تماماً**
- ✅ **يمكن فحصه** - يمكن فتح الملف

#### العيوب:
- ❌ **يحتاج تنظيف** - يجب حذف الملف بعد الاختبارات

#### الإعداد:
```xml
<!-- phpunit.xml -->
<env name="DB_CONNECTION" value="sqlite"/>
<env name="DB_DATABASE" value="database/testing.sqlite"/>
```

---

### الخيار 3: MySQL قاعدة بيانات منفصلة ⭐ **للاختبارات المعقدة**

#### المميزات:
- ✅ **يدعم جميع MySQL features**
- ✅ **مشابه لـ Production**
- ✅ **مثالي للاختبارات المعقدة**

#### العيوب:
- ❌ **أبطأ** - يحتاج اتصال بـ MySQL
- ❌ **يحتاج إعداد** - إنشاء قاعدة بيانات

#### الإعداد:
```xml
<!-- phpunit.xml -->
<env name="DB_CONNECTION" value="mysql"/>
<env name="DB_DATABASE" value="mnjuz_testing"/>
<env name="DB_USERNAME" value="root"/>
<env name="DB_PASSWORD" value=""/>
```

---

## 🚀 التوصية

### للاختبارات البسيطة (API Auth):
**استخدم SQLite In-Memory** - سريع ونظيف

### للاختبارات المعقدة:
**استخدم MySQL منفصل** - يدعم جميع الميزات

---

## 📝 الإعداد الموصى به

### الخطوة 1: تحديث phpunit.xml

```xml
<php>
    <env name="APP_ENV" value="testing"/>
    <env name="DB_CONNECTION" value="sqlite"/>
    <env name="DB_DATABASE" value=":memory:"/>
    <!-- أو -->
    <!-- <env name="DB_CONNECTION" value="mysql"/> -->
    <!-- <env name="DB_DATABASE" value="mnjuz_testing"/> -->
</php>
```

### الخطوة 2: إنشاء قاعدة بيانات اختبار (إذا استخدمت MySQL)

```bash
mysql -u root -p
CREATE DATABASE mnjuz_testing;
```

### الخطوة 3: تشغيل Migrations في الاختبارات

Laravel يقوم بذلك تلقائياً مع `RefreshDatabase` trait!

---

## 🔄 كيف يعمل RefreshDatabase؟

```php
use RefreshDatabase; // في Test class

// يقوم بـ:
// 1. تشغيل جميع migrations
// 2. تنظيف البيانات بعد كل test
// 3. إعادة تشغيل migrations قبل كل test
```

---

## ✅ الخلاصة

| الخيار | السرعة | التعقيد | الاستخدام |
|--------|--------|---------|-----------|
| SQLite In-Memory | ⚡⚡⚡ | بسيط | ✅ موصى به |
| SQLite File | ⚡⚡ | بسيط | ✅ جيد |
| MySQL منفصل | ⚡ | متوسط | ✅ للاختبارات المعقدة |
| نفس قاعدة البيانات | ⚡ | خطير | ❌ غير موصى به |
