# نقاط جديدة للتطبيق: تذاكر الدعم وإعداد التحقّق

آخر تحديث: 2026-09-19

ثلاث إضافات:

1. إعداد **عمليات التحقق** داخل `/profile` — كان غائبًا.
2. **تذاكر الدعم** — لم يكن لها endpoint إطلاقًا.
3. التعليقات وتغيير الحالة والأولوية على تذكرة الدعم.

كل ما هنا يتبع القواعد العامّة في `MOBILE_NEW_ENDPOINTS.md`: نفس الـ Bearer token، ونفس غلاف الرد (`statusCode` · `success` · `message` · `data`)، ونفس ترويسة `Accept-Language`.

---

## تنبيه: هناك نوعان من «التذاكر» ولا علاقة بينهما

| | تذاكر المحادثات | تذاكر الدعم |
|---|---|---|
| المعنى | محادثة عميل: مفتوحة أو مغلقة، ومسندة لموظّف | طلب **تفتحه أنت** لفريق الدعم عندنا |
| الـ endpoint | `GET /api/v1/tickets` | `GET /api/v1/support-tickets` |
| الشاشة في الويب | «التذاكر» | «الدعم» |

الأولى موجودة من قبل ولم تتغيّر. والجديد كلّه في الثانية.

---

## 1) إعداد عمليات التحقق

يقابل تبويب **«عمليات التحقق»** في نافذة الملف الشخصي بالويب. إعداد واحد: هل يُطلب من المستخدم تحقّق واحد قبل دخول لوحة التحكّم؟

**لا تخلط بينه وبين** `POST /verification/send` و`POST /verification/confirm` — تلك **تُنفّذ** عملية التحقّق (إرسال الرمز وتأكيده). وهذا يقرّر هل يُطلب التحقّق أصلًا.

### القراءة

```
GET /api/v1/profile
```

ثلاثة حقول جديدة في `data`:

```json
{
  "verification_enabled": true,
  "is_verified": true,
  "verification_available": true
}
```

| الحقل | المعنى |
|---|---|
| `verification_enabled` | الإعداد نفسه — هل التحقّق مطلوب |
| `is_verified` | هل تحقّق المستخدم فعلًا |
| `verification_available` | **هل تُظهر الخيار أصلًا** |

`verification_available` تتبع إضافة **Google Authenticator**: الويب يُخفي التبويب كلّه متى كانت مطفأة. افعل مثله — إن كانت `false` فلا تعرض الخيار.

### التعديل

```
PUT /api/v1/profile
```

```json
{
  "first_name": "أحمد",
  "last_name": "صلاح",
  "email": "ahmed@example.com",
  "verification_enabled": true
}
```

| النقطة | السلوك |
|---|---|
| الحقل اختياري | إن لم ترسله **لا يُمسّ** — احفظ شاشة الملف الشخصي بلا قلق |
| القيمة | `true` أو `false` فقط، وغيرها **400** |
| الإضافة مطفأة | يُتجاهل الحقل بصمت، فلا يُحفظ إعداد لا أثر له |

`first_name` و`last_name` و`email` تبقى **مطلوبة** كما كانت.

---

## 2) تذاكر الدعم

### الجلب

```
GET /api/v1/support-tickets?page=1&per_page=10
```

```json
{
  "data": {
    "items": [
      {
        "uuid": "3b1f…",
        "reference": "SUP-000012-260919",
        "subject": "الرسائل لا تصل",
        "message": "جرّبنا الإرسال ولم يصل شيء",
        "status": "open",
        "priority": "high",
        "category": { "id": 2, "name": "فوترة" },
        "created_at": "2026-09-19 10:14:00",
        "updated_at": "2026-09-19 10:14:00"
      }
    ],
    "pagination": { "page": 1, "per_page": 10, "total": 1, "last_page": 1 },
    "categories": [ { "id": 2, "name": "فوترة" } ],
    "statuses": ["open", "pending", "resolved", "closed"],
    "priorities": ["critical", "high", "medium", "low"],
    "waz_tickets": [],
    "waz_available": false
  }
}
```

**`items`** تذاكر المستخدم الحالي وحده — لا تذاكر بقيّة أعضاء المنشأة.

**`categories`** و**`statuses`** و**`priorities`** قوائم الاختيار جاهزة في نفس الرد، فلا تحتاج استدعاءً آخر ولا تكتبها في كود التطبيق.

**`waz_tickets`** و**`waz_available`** — اقرأ الفقرة التالية.

### فريق الدعم يعمل في نظام آخر

التذكرة تُفتح في مكانين: عندنا، وفي **واز أعمال** حيث يعمل فريق الدعم فعلًا. ولذلك:

| الحقل | المعنى |
|---|---|
| `waz_available: true` | الاتصال بنظام الدعم قائم، و`waz_tickets` تحمل التذاكر كما يراها الفريق |
| `waz_available: false` | تعذّر الوصول الآن — اعرض `items` وحدها ونبّه أن حالة الدعم قد تكون غير محدّثة |

`waz_tickets` مصدر الحقيقة للحالة والردود، و`items` نسختنا المحلّية. اعرض الاثنتين كما تفعل الويب.

### تذكرة واحدة

```
GET /api/v1/support-tickets/{uuid}
```

نفس الحقول، ومعها `comments`:

```json
{
  "comments": [
    {
      "id": 5,
      "message": "ما زالت المشكلة قائمة",
      "seen": true,
      "created_at": "2026-09-19 11:02:00",
      "user": { "id": 9, "name": "أحمد صلاح", "is_me": true }
    }
  ]
}
```

`is_me` تُغنيك عن مقارنة المعرّفات: اعرض الرد على اليمين أو اليسار بحسبها.

**فتح التذكرة يُعلّم ردودها مقروءة** تلقائيًا — لا تحتاج استدعاءً منفصلًا.

### الإنشاء

```
POST /api/v1/support-tickets
```

```json
{
  "category": 2,
  "subject": "فاتورة غير صحيحة",
  "message": "المبلغ المخصوم أكبر من المتوقّع"
}
```

الثلاثة **مطلوبة**. `category` رقم من `categories`. و`subject` و`message` حدّهما 1024 حرفًا.

الرد يحمل التذكرة كاملة ومعها:

```json
"synced_with_support": true
```

| القيمة | ماذا تعرض للمستخدم |
|---|---|
| `true` | «تم إرسال التذكرة» |
| `false` | «حُفظت تذكرتك، وقد يتأخّر وصولها للدعم» |

**التذكرة تُحفظ في الحالتين.** `false` لا تعني الفشل — تعني أن المزامنة مع نظام الدعم تأخّرت. لا تطلب من المستخدم إعادة الإرسال، فستُنشأ تذكرة مكرّرة.

### التعليق

```
POST /api/v1/support-tickets/{uuid}/comment
{ "message": "ما زالت المشكلة قائمة" }
```

مطلوب، وحدّه 1024 حرفًا. الرد يحمل التذكرة ومعها `comments` محدّثة — لا تحتاج جلبًا بعده.

### الحالة والأولوية

```
POST /api/v1/support-tickets/{uuid}/status
{ "status": "closed" }

POST /api/v1/support-tickets/{uuid}/priority
{ "priority": "high" }
```

القيم المقبولة هي ما يصلك في `statuses` و`priorities`، وغيرها **400**.

---

## شروط الاستخدام

| النقطة | من يستطيع | ملاحظات |
|---|---|---|
| `GET /profile` | الجميع | لكلٍّ ملفّه هو |
| `PUT /profile` | الجميع | `verification_enabled` اختياري |
| `GET /support-tickets` | الجميع | تذاكر المستخدم الحالي وحده |
| `GET /support-tickets/{uuid}` | صاحب التذكرة | **404** لتذكرة غيره |
| `POST /support-tickets` | الجميع — بما فيهم الموظّف | الدعم ليس حكرًا على المالك |
| `comment` · `status` · `priority` | صاحب التذكرة | **404** لتذكرة غيره |

**لا شرط باقة على أيٍّ منها.**

وتذكرة غيرك تعود **404 لا 403** — عمدًا، كي لا يُستدلّ على وجودها.

---

## أخطاء متوقّعة

| الرمز | متى |
|---|---|
| `400` | حقل ناقص · فئة غير موجودة · حالة أو أولوية خارج المسموح · قيمة غير منطقية لـ `verification_enabled` |
| `401` | بلا token أو منتهٍ |
| `404` | تذكرة غير موجودة أو ليست لك |

تفاصيل الحقول في `errors` كالمعتاد، ونصّ `message` مترجم وجاهز للعرض.
