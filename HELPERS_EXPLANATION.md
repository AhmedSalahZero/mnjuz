# شرح مشكلة getApiLang() - لماذا حدثت المشكلة؟

## 🤔 السؤال: لماذا حدثت المشكلة رغم أنك عرّفتها مرة واحدة فقط؟

### السبب: الملف يتم تحميله أكثر من مرة!

---

## 📋 كيف يعمل الكود الحالي:

### 1. في `AppServiceProvider.php`:
```php
public function boot(): void
{
    include app_path('Helpers/Helpers.php'); // يتم تحميل الملف هنا
}
```

### 2. المشكلة:
- `boot()` method يتم استدعاؤه **أكثر من مرة** في بيئة الاختبار
- كل مرة يتم استدعاء `boot()` → يتم تحميل الملف → يتم تعريف الدالة مرة أخرى
- النتيجة: **Cannot redeclare function**

---

## 🔍 متى يتم استدعاء `boot()` أكثر من مرة؟

### في بيئة الاختبار:
1. ✅ عند بدء الاختبار الأول
2. ✅ عند بدء الاختبار الثاني
3. ✅ عند بدء الاختبار الثالث
4. ... وهكذا

### في بيئة الإنتاج:
- عادة يتم استدعاؤه مرة واحدة فقط
- لكن في بعض الحالات (مثل queue workers) قد يتم استدعاؤه أكثر من مرة

---

## ✅ الحل (الذي تم تطبيقه):

```php
<?php 

if (!function_exists('getApiLang')) {
    function getApiLang(): string
    {
        return app()->getLocale() ?? 'ar';
    }
}
```

### كيف يعمل:
- ✅ يتحقق أولاً: هل الدالة موجودة؟
- ✅ إذا كانت موجودة: لا يعرّفها مرة أخرى
- ✅ إذا لم تكن موجودة: يعرّفها

---

## 🎯 الخلاصة:

| السؤال | الإجابة |
|--------|---------|
| هل عرّفتها مرة واحدة؟ | ✅ نعم، في الملف |
| هل الملف يتم تحميله مرة واحدة؟ | ❌ لا، يتم تحميله أكثر من مرة |
| لماذا؟ | لأن `boot()` يتم استدعاؤه أكثر من مرة |
| الحل؟ | ✅ `function_exists()` يمنع إعادة التعريف |

---

## 💡 بديل أفضل (اختياري):

يمكنك أيضاً استخدام **autoloading** بدلاً من `include`:

### في `composer.json`:
```json
"autoload": {
    "files": [
        "app/Helpers/Helpers.php"
    ]
}
```

ثم:
```bash
composer dump-autoload
```

لكن الحل الحالي (`function_exists`) **أفضل وأبسط**! ✅
